{
  "name": "@agent-workflow/examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint src",
    "workflow:smoke": "node dist/workflow-live-smoke.js"
  },
  "dependencies": {
    "@agent-workflow/server": "workspace:*"
  }
}
