packages:
  - shared
  - server
  - web
  - desktop
  - examples

allowBuilds:
  esbuild: true
  msw: false

# Hard-fail installs on unsupported Node instead of warning, so an out-of-range
# Node stops at install time rather than surfacing as confusing downstream errors.
engineStrict: true
