{
  "name": "@agent-workflow/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint src",
    "start": "electron .",
    "smoke": "electron-rebuild --force --which-module better-sqlite3 --module-dir ../server && electron . --smoke"
  },
  "devDependencies": {
    "@electron/rebuild": "4.2.0",
    "electron": "43.1.0"
  }
}
