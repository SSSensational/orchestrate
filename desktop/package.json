{
  "name": "@agent-workflow/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint src",
    "start": "electron .",
    "smoke": "electron . --smoke"
  },
  "devDependencies": {
    "electron": "43.1.0"
  }
}
